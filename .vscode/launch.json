{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Crawler",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/crawler",
            "cwd": "${workspaceFolder}"
        },
    ]
}